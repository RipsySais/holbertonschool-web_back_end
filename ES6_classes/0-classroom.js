// 0-classroom.js

// Définition de la classe ClassRoom et export par défaut
export default class ClassRoom {
  /**
   * Constructeur de la classe
   * @param {Number} maxStudentsSize - nombre maximum d'étudiants
   */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}

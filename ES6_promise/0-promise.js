/**
 * Retourne une Promise.
 * @returns {Promise} - Une Promise.
 */
export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // La Promise est créée, mais elle n'est ni résolue ni rejetée ici.
    // Cela permet de vérifier que la fonction retourne bien une Promise.
  });
}
